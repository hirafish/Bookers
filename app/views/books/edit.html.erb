<!DOCTYPE HTML>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <title>edit</title>
  </head>
</html>

<body class="edit_body">
  
  <h2>Editing Book</h2>
  <%= form_with model: @book do |f| %>

    <p>Title</p>
    <%= f.text_field :title %>
    
    <p>Body</p>
    <%= f.text_area :body %><br>
    <%= f.submit 'Update Book' %>

  <% end %>
  
  <%= link_to "Show", book_path(@book.id) %> <span>|</span> <%= link_to "Back", "/books" %>
  
</body>